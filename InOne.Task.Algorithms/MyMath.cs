using System;
using System.Collections.Generic;
using System.Text;

namespace InOne.Task.Algorithms
{
    public class MyMath
    {
        public static int Pow10(int number) => (int)Math.Pow(10, number);
        public static int IntLog10(int number) => (int)Math.Log10(number);
        public static int IntLog(int number) => (int)Math.Log(number);
    }
}
